[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "bookoftench"
version = "0.0.0"
description = "Local BOT package"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["bookoftench"]
